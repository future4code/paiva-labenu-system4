export type userFull = {
   id: string
   name: string
   nickname: string
   email: string
   zipcode: string
   number: number
   address?: string
   street: string
   neighborhood: string
   city: string
   state: string
}
export type addressInfo = {
   street: string
   neighborhood: string
   city: string
   state: string
}